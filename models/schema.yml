sources:
  - name: source1
    schema: raw
    tables:
      - name: batch_claims_staging
      - name: streaming_claims_staging

models:
  - name: stg_allclaims
    columns:
      - name: claim_id
        tests:
          - not_null
          - unique
         